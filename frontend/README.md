# Resibought Frontend

## Developing

Use `npm install --legacy-peer-deps ` to start a development server:

```sh
npm run dev

# or start the server and open the app in a new browser tab
npm run dev -- --open

# to have a front end workshop
npm run storybook
```
