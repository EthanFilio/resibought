# Resibought

## Repository Structure

```text
resibought/
├── backend/   # FastAPI server (API + inference endpoints)
├── frontend/  # SvelteKit app (UI)
├── ml/        # Training code, experiments, and pipelines
├── data/      # Datasets (raw/processed)
├── models/    # Trained model files and checkpoints
└── README.md
```
