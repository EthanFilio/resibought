# Resibought
## Project Structure
resibought/
├── backend/ # FastAPI server (API + inference)
├── frontend/ # SvelteKit app (UI)
├── ml/ # Training code (models + scripts)
├── data/ # Datasets (SROIE, CORD, etc.)
└── models/ # Trained model files
